{
  "name": "demo-test652",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A deliberately small app used to exercise Phantom end to end.",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
